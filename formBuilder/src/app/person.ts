export class Person {
  firstName: string;
  lastName: string;
  gender: string;
  degree: string;
  constructor(firstName:string, lastName:string, gender: string, degree: string){

    this.firstName=firstName;
    this.gender = gender;
    this.lastName=lastName;
    this.degree=degree;
  }
}
